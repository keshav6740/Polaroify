{
    "name": "spotpost",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "concurrently \"node --env-file=.env server.js\" \"node -e \\\"const net=require('node:net');const tryConnect=()=>{const s=net.connect(8787,'127.0.0.1');s.on('connect',()=>{s.end();process.exit(0);});s.on('error',()=>setTimeout(tryConnect,200));};tryConnect();\\\" && vite\"",
        "dev:client": "vite",
        "dev:server": "node --env-file=.env server.js",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "dom-to-image": "^2.6.0",
        "dotenv": "^17.3.1",
        "express": "^4.21.2"
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "vite": "^7.1.0"
    }
}
